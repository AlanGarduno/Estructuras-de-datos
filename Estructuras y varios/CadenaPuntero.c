#include <stdio.h>
#include <stdlib.h>

int main()
{
	char *cadena = (char*)malloc(sizeof(char*10));
	char *cadE = (char*)malloc(sizeof(char*10));
	char *aux = cadena;
	printf("Introduce una cadena\n");
	scanf("%s\n",cadena);
	printf("Introduce la palabra que deseas quitar\n");
	scanf("%s\n",cadE);
	while(cadena != NULL)
	{
		
	}
	return 0;
}